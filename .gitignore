# Electron and deps — 278MB, all reproducible from package-lock.json
node_modules/

# Electron build output
dist/
out/

.DS_Store
*.log
npm-debug.log*

# Installed packages: fetched at runtime, never committed
packages/
